{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // this is important so declaration files are created alongside
    // of the build so the library can be imported
    "noEmit": false,
    "emitDeclarationOnly": true,
    "declaration": true,
    "declarationDir": "dist-lib"
  },
  "include": ["src/features/index.ts"]
}
